import globalStyles from "../../styles/globalStyles";

const initialState = {
    backgroundColorDark: globalStyles.dark,
    textColorWhite: globalStyles.white,
};

const reducerStyles = (state = initialState, action) => {
    return state;
};

export default reducerStyles;
